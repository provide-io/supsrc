# wrknv configuration for supsrc

[project]
name = "supsrc"
description = "Automated Git commit/push utility based on filesystem events"

[tools]
uv = "latest"

[siblings]
patterns = [
    "wrknv",  # Environment manager
]

[settings]
python_version = ">=3.11"